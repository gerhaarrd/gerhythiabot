[project]
name = "rhythia-discord-bot"
version = "0.1.0"
description = "Discord bot for the Rhythia API"
requires-python = ">=3.13"
dependencies = [
    "discord.py>=2.4",
    "python-dotenv>=1.0",
    "requests>=2.32",
]

[tool.uv]
package = false
